div.navbar.navbar-default.navbar-static-top(style="margin: 0")
    div.container
        div.navbar-header
            button.navbar-toggle(type='button', ng-click='isCollapsed = !isCollapsed')
                span.sr-only Toggle navigation
                span.icon-bar
                span.icon-bar
                span.icon-bar
            a.navbar-brand(href='/') 8Cakes
                div(style="font-size: 10px; line-height: 11px;") ...now I'm fat

header#banner
    .container.page-header
        h1 Tracked deliveries
        div(ng-if="!model.firstLoad")
            span(ng-if="model.deliveries.length") Showing data for {{model.deliveries.length}} deliveries
            span(ng-if="model.deliveries.length && model.loading") &nbsp;and loading
            i.fa.fa-cog.fa-spin.fa-fw(ng-if="model.deliveries.length && model.loading")
            span(ng-if="!model.deliveries.length") No deliveries found!
        div(ng-if="model.firstLoad") Loading data
            i.fa.fa-cog.fa-spin.fa-fw

.container(ng-if="!model.firstLoad && model.deliveries.length")
    div#charts
        div(ng-repeat="c in model.charts")
            div.chart(dc-bar-chart="c")
footer.footer
    .container
        p
            | delolap v0.0.1
            = ' | '
            a(href='http://www.9cookies.com') 9Cookies
