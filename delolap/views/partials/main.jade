div.navbar.navbar-default.navbar-static-top(style="margin: 0")
    div.container
        div.navbar-header
            button.navbar-toggle(type='button', ng-click='isCollapsed = !isCollapsed')
                span.sr-only Toggle navigation
                span.icon-bar
                span.icon-bar
                span.icon-bar
            a.navbar-brand(href='/') 8Cakes
                div(style="font-size: 10px; line-height: 11px;") ...now I'm fat

header#banner
    .container.page-header
        h1 Tracked deliveries
        div(ng-if="!model.loading") Showing data for {{model.deliveries.length}} deliveries
        div(ng-if="model.loading") Loading data...

.container
    div#charts
        div(ng-repeat="c in model.charts")
            div.chart(bar-chart chart-model="c.model" style="float: left;")
                div.title {{c.title}}
footer.footer
    .container
        p
            | delolap v0.0.1
            = ' | '
            a(href='http://www.9cookies.com') 9Cookies
